<div id="<%= dom_id user %>">
  <p>
    <strong>Name:</strong>
    <%= user.name %>
  </p>

  <p>
    <strong>Email:</strong>
    <%= user.email %>
  </p>

  <div>
    <% user.microposts.each do |post| %>
    <p>
      <%= post.content %>
    </p>
    <% end %>
  </div>

</div>
